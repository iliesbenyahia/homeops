externalSecrets:
  renovate-secret:
    data:
      - secretKey: GITHUB_COM_USER
        remoteRef:
          key: github-com-user
      - secretKey: GITHUB_COM_TOKEN
        remoteRef:
          key: github-com-token
      - secretKey: RENOVATE_TOKEN
        remoteRef:
          key: github-com-token

services:
  renovate-operator:
    create: false        
    expose:
      type: lan
      port: 8000       